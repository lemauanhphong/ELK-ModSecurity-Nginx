input {
  file {
    path => "/home/modsec/audit.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

output {
  elasticsearch {
    hosts => ["http://es:9200"]
    index => "modsec"
  }
  stdout {
    codec => rubydebug
  }
}