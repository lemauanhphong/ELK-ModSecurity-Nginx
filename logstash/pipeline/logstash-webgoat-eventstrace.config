input {
  file {
    path => "/home/webgoat/EventsTrace.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

output {
  elasticsearch {
    hosts => ["http://es:9200"]
    index => "eventstrace"
  }
  stdout {
    codec => rubydebug
  }
}